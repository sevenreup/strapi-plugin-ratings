import { Tabs as StrapiTabs } from '@strapi/design-system';


export const Tabs = StrapiTabs.Root;

export const Tab = StrapiTabs.Trigger;

export const TabList = StrapiTabs.List;

export const TabContent  = StrapiTabs.Content;
