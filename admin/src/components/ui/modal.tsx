import { Modal as StrapiModal } from '@strapi/design-system';


export const ModalLayout = StrapiModal.Root;

export const ModalHeader = StrapiModal.Header;

export const ModalFooter = StrapiModal.Footer;

export const ModalBody = StrapiModal.Content;

export const ModalClose = StrapiModal.Close;
